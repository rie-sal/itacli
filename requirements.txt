# Active dependencies. Easiest install: ./install.sh  (creates a venv + model).
# The core app runs on the stdlib alone; these add smarter tagging + verbs.
spacy>=3.8
mlconjug3
scikit-learn==1.6.1     # pin: mlconjug3's bundled model unpickles only on 1.6.x
# Italian spaCy model is NOT a normal pip package - install it with:
#   python -m spacy download it_core_news_md
# Grammar still works WITHOUT these (built-in rules cover articles + regular
# plurals); spaCy/mlconjug3 add accurate tagging + verb conjugation.

# --- Later / optional ---
# Morph-it! lexicon     # free Italian morphology data; full-coverage inflection
#                       # http://docs.sslmit.unibo.it/doku.php?id=resources:morph-it

# --- Content sourcing ---
# requests
# beautifulsoup4        # scrape Gutenberg / Wikisource / grammar sites
# praw                  # Reddit API
# yt-dlp                # video + transcripts (listening)

# --- Language detection (native-speaker sub filtering) ---
# fasttext / langid

# --- Difficulty / CEFR tagging (Tier 2 encoder model) ---
# transformers
# torch

# --- Anki bridge ---
# genanki               # generate .apkg
# (AnkiConnect is an Anki add-on reached over HTTP; no pip package needed)

# --- Terminal UI (optional upgrade from stdlib) ---
# rich / textual
